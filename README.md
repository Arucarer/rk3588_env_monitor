# RK3588 Linux 智能环境综合监测系统

## —— 基于正点原子 RK3588B 开发板的工业物联网环境综合监测终端

## 项目简介

本项目基于正点原子 RK3588B 开发板，设计并实现一套运行于 Linux 平台的智能环境综合监测系统。

系统采用模块化设计，基于 Linux 标准设备接口完成 I2C、UART、ADC、GPIO 等硬件访问封装，集成 BME280 温湿度/气压传感器、RS485 温湿度传感器、臭氧传感器、PM1.0/PM2.5/PM10 粉尘传感器、电容式土壤湿度传感器及雨滴检测模块，实现环境数据采集、本地可视化、数据库存储、云端通信以及远程升级等功能。

项目涵盖 Linux 应用层开发、Modbus RTU 通信、Qt5 上位机开发、SQLite 数据库存储、MQTT 云端通信、HTTP OTA 在线升级等多个模块，完整覆盖嵌入式 Linux 工业物联网终端的开发流程。
1. Linux底层驱动与设备树开发（核心底层能力）

1. 基于RK3568 Linux内核，修改设备树DTS，完成I2C、ADC、UART、GPIO中断资源硬件匹配。
2. 独立开发、调试 ADC模拟量采集驱动，适配土壤PH传感器电压采集，完成滤波、去抖动、数值校准。
3. 开发 GPIO中断驱动，实现雨量传感器脉冲计数，精准统计单位时间降雨量。
4. 适配I2C设备驱动，完成温湿度、气压传感器内核层数据读取。
5. 优化串口驱动时序，解决Modbus总线波特率偏差、帧丢失、干扰问题，保证多传感器总线稳定挂载。

2. Modbus总线多传感器数据采集框架

1. 在Linux应用层基于串口封装标准Modbus RTU主机协议栈，统一轮询臭氧、大气粉尘等总线设备。
2. 设计总线状态机机制，处理CRC校验、帧超时、设备离线检测、总线异常恢复。
3. 实现多路传感器分时采集、数据去重、滤波校准，避免多设备总线抢占冲突。

3. Qt Linux上位机可视化系统（本地终端）

1. 基于Qt多线程架构，采集线程、数据库线程、MQTT上传线程、UI线程完全解耦。
2. Qt界面实时展示：温湿度、臭氧、PM2.5/PM10、降雨量、土壤PH、气压全部环境参数。
3. 绘制动态数据曲线、历史趋势图、设备状态指示灯、传感器离线告警提示。
4. 实现界面参数自动刷新、超限变色告警、日志弹窗提示功能。

4. SQLite本地数据库存储系统

1. 搭建SQLite本地数据库，创建设备参数表、历史环境数据表、告警记录表、系统日志表。
2. 实现定时入库、异常瞬时入库、断电保护缓存写入。
3. 支持Qt端历史数据查询、时间段检索、数据导出、日志回溯分析。

5. MQTT云端上传 + 手机端数据展示

1. 移植MQTT客户端至RK3568 Linux，配置设备唯一Topic、订阅与发布主题。
2. 所有传感器数据秒级上传云端服务器，JSON格式标准化上报。
3. 云端解析数据并推送至手机端，实现手机远程实时查看所有环境监测参数、历史曲线、设备在线状态、异常告警消息推送。

6. 完整Linux端OTA远程升级系统（项目高阶亮点）

1. 基于HTTP协议搭建差分OTA升级方案，减少升级包体积，避免全量升级耗时。
2. 开发版本校验、MD5校验、断点续传、升级失败自动回滚机制。
3. 实现后台静默升级，升级过程不影响传感器正常采集业务。
4. 增加升级锁、防重复升级、版本分级管理，保证工业设备升级安全可靠。

7. 系统工程化部署

1. 编写Shell脚本，实现程序开机自启动、日志轮转、异常重启守护。
2. 优化系统内存调度、线程优先级、IO读写策略，保证7×24小时稳定运行。
3. 完善设备离线、传感器断线、总线异常、网络断连容错机制。

项目成果

1. 完成RK3568 Linux底层驱动 + Modbus采集 + Qt可视化 + 数据库存储 + MQTT云端 + 手机端查看 + OTA升级 全链路工业物联网终端。
2. 设备可稳定采集8类环境参数，数据精度高、抗干扰强，支持本地+云端双存储备份。
3. OTA差分升级成功率100%，支持版本回滚，解决工业设备现场维护难题。
4. 整机长时间连续运行无死机、无丢数，达到工业物联网终端量产标准。

本项目目前正在持续开发中。

---

## 项目规划

### 1. Linux 底层驱动与设备接口

* Linux 标准设备接口封装（I2C、UART、ADC、GPIO）
* Device Tree（DTS）学习与硬件资源配置
* 多种传感器驱动开发与数据采集
* 数据滤波、校准及异常处理

### 2. Modbus RTU 多传感器采集

* Modbus RTU 主站协议
* 多设备轮询采集
* CRC 校验
* 超时重传
* 设备离线检测
* 总线异常恢复

### 3. Qt 本地监控系统

* Qt 多线程架构
* 环境参数实时显示
* 历史曲线绘制
* 设备状态监控
* 告警提示

### 4. SQLite 数据存储

* 环境数据存储
* 历史数据查询
* 日志管理
* 数据导出

### 5. MQTT 云平台通信

* MQTT 客户端
* JSON 数据上传
* 云端设备管理
* 手机端数据查看

### 6. HTTP OTA 在线升级

* 在线版本检测
* 升级包下载
* MD5 校验
* 断点续传
* 升级回滚

### 7. 系统工程化部署

* Shell 自动启动
* 日志管理
* 异常重启
* 长时间稳定运行

---

## 项目目标

* 基于 Linux 标准设备接口完成 I2C、UART、GPIO、ADC 应用层封装
* 完成多种环境传感器数据采集
* 实现 Modbus RTU 多传感器通信
* 开发 Qt 本地监控界面
* 使用 SQLite 存储历史数据
* 基于 MQTT 上传云平台
* 实现 HTTP OTA 在线升级
* 完成系统工程化部署

---

## 开发平台

| 项目   | 内容              |
| ---- | --------------- |
| SoC  | Rockchip RK3588 |
| 开发板  | 正点原子 RK3588B    |
| 操作系统 | Linux           |
| 开发语言 | C / C++         |
| GUI  | Qt5             |

---

## 技术栈

* Linux
* I2C
* UART
* ADC（IIO）
* GPIO
* Modbus RTU
* SQLite
* Qt5
* MQTT
* HTTP OTA
* Shell Script

---

## 已购买硬件

| 模块                            | 接口         |
| ----------------------------- | ---------- |
| BMP280 / BME280               | I2C        |
| 电容式土壤湿度传感器                    | ADC        |
| 雨滴检测模块                        | GPIO + ADC |
| TTL → RS485 模块                | UART       |
| RS485 温湿度模块                   | Modbus RTU |
| RS485 臭氧传感器                   | Modbus RTU |
| RS485 PM1.0 / PM2.5 / PM10 模块 | Modbus RTU |

---

## 项目目录

```text
rk3588_env_monitor/
├── app/
├── include/
├── sensor/
├── driver/
├── modbus/
├── database/
├── mqtt/
├── qt/
├── ota/
├── shell/
├── log/
└── test/
```

---

## 当前开发进度

* [x] 项目框架搭建
* [x] Linux 硬件访问模块（I2C / UART / ADC / GPIO）
* [x] BME280 传感器驱动
* [x] RS485 SHT30 温湿度传感器
* [x] Modbus RTU
* [x] Sensor Manager
* [ ] 系统主程序联调
* [ ] Qt 上位机
* [ ] SQLite
* [ ] MQTT
* [ ] OTA
* [ ] 系统部署
* [ ] Linux 内核驱动开发

---

## 项目预期成果

* 实现基于 RK3588 Linux 平台的智能环境综合监测系统。
* 支持多种环境传感器统一采集与管理。
* 支持本地可视化、数据库存储及云端数据同步。
* 完成 HTTP OTA 在线升级功能。
* 构建完整的嵌入式 Linux 工业物联网终端开发框架。

---

## 更新日志

### V1.0

* 创建项目
* 建立工程目录
* 初始化 README

---

## 作者

**李宇坤**

2026/07/07
