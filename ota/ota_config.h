/******************************************************************************
 * @file    ota_config.h
 * @brief   OTA远程升级配置参数
 *
 * @author  李宇坤
 * @date    2026-08
 * @version V1.0
 *
 * @details
 * 本文件定义OTA升级系统使用的固定配置参数，包括：
 * 1. OTA服务器地址；
 * 2. version.json下载地址及本地保存路径；
 * 3. OTA升级包、临时文件及备份文件路径；
 * 4. 本地版本文件及升级状态文件路径；
 * 5. OTA锁文件路径；
 * 6. HTTP超时及下载参数。
 ******************************************************************************/

 #ifndef __OTA_CONFIG_H__
 #define __OTA_CONFIG_H__
 
 
 /******************************************************************************
  * 设备信息
  ******************************************************************************/
 
 /* 当前设备型号，必须与服务器version.json中的device字段一致 */
 #define OTA_DEVICE_NAME             "rk3588_env_monitor"
 
 
 /******************************************************************************
  * OTA服务器配置
  ******************************************************************************/
 
 /* OTA服务器基础地址 */
 #define OTA_SERVER_URL              "http://192.168.88.100:8080"
 
 /* 服务器version.json地址 */
 #define OTA_VERSION_URL             OTA_SERVER_URL "/version.json"
 
 
 /******************************************************************************
  * OTA本地目录
  ******************************************************************************/
 
 /* OTA工作目录 */
 #define OTA_WORK_DIR                "/tmp/rk3588_ota"
 
 /* 下载到本地的version.json */
 #define OTA_VERSION_FILE            OTA_WORK_DIR "/version.json"
 
 /* 下载中的升级包 */
 #define OTA_DOWNLOAD_FILE           OTA_WORK_DIR "/update.part"
 
 /* 差分补丁文件 */
 #define OTA_PATCH_FILE              OTA_WORK_DIR "/update.patch"
 
 /* 差分合成后的新程序 */
 #define OTA_NEW_FILE                OTA_WORK_DIR "/env_monitor.new"
 
 
 /******************************************************************************
  * 当前程序与备份
  ******************************************************************************/
 
 /* 当前运行程序路径 */
 #define OTA_CURRENT_PROGRAM         "/usr/local/bin/env_monitor"
 
 /* 当前程序备份路径 */
 #define OTA_BACKUP_FILE             OTA_WORK_DIR "/env_monitor.bak"
 
 
 /******************************************************************************
  * OTA版本及状态文件
  ******************************************************************************/
 
 /* 当前程序版本记录文件 */
 #define OTA_LOCAL_VERSION_FILE      "/var/lib/rk3588_env_monitor/version"                  //本地版本号文件
 
 /* OTA升级状态记录文件 */
 #define OTA_STATUS_FILE             "/var/lib/rk3588_env_monitor/ota_status"
 
 /* OTA升级成功标记文件 */
 #define OTA_SUCCESS_FILE            "/var/lib/rk3588_env_monitor/ota_success"
 
 /* 最近一次升级失败版本记录 */
 #define OTA_FAILED_VERSION_FILE     "/var/lib/rk3588_env_monitor/ota_failed_version"
 
 
 /******************************************************************************
  * OTA升级锁
  ******************************************************************************/
 
 /* OTA锁文件，防止多个升级任务同时运行 */
 #define OTA_LOCK_FILE               "/tmp/rk3588_ota.lock"
 
 
 /******************************************************************************
  * HTTP参数
  ******************************************************************************/
 
 /* HTTP连接超时时间，单位：秒 */
 #define OTA_HTTP_CONNECT_TIMEOUT    10
 
 /* HTTP总请求超时时间，单位：秒 */
 #define OTA_HTTP_TIMEOUT            300
 
 /* HTTP下载缓冲区大小 */
 #define OTA_HTTP_BUFFER_SIZE        4096
 
 
 /******************************************************************************
  * OTA健康检查配置
  ******************************************************************************/
 
 /* 新版本启动后的健康检查等待时间，单位：秒 */
 #define OTA_HEALTH_CHECK_DELAY      10
 
 /* 健康检查最大重试次数 */
 #define OTA_HEALTH_CHECK_RETRY      3
 
 
 /******************************************************************************
  * OTA权限配置
  ******************************************************************************/
 
 /* env_monitor程序执行权限 */
 #define OTA_PROGRAM_MODE            0755
 
 
 #endif